//

// Representation of a node

function Node(val) {
  this.val = val;
  this.next = null;
}

// Create a new node

let newNode = new Node(5);

//

// Representation of Linked List

function MyLinkedList() {
  this.head = null;
  this.size = 0;
}
